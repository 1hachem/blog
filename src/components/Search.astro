---
import Search from 'astro-pagefind/components/Search.astro';
---

<div class="search">
	<Search
		id="site-search"
		className="pagefind-ui"
		uiOptions={{ showImages: false, excerptLength: 15, resetStyles: false }}
	/>
</div>

<style is:global>
	/* Inline search on the blog listing, sitting above the category filter */
	.search {
		position: relative;
		margin-bottom: 1rem;
	}
	.search .pagefind-ui {
		--pagefind-ui-primary: var(--fg);
		--pagefind-ui-text: var(--fg);
		--pagefind-ui-background: var(--bg);
		--pagefind-ui-border: transparent;
		--pagefind-ui-tag: var(--bg);
		--pagefind-ui-border-width: 0px;
		--pagefind-ui-border-radius: 0px;
		--pagefind-ui-font: var(--font-mono);
	}
	.search .pagefind-ui__form::before {
		opacity: 0.45;
		width: 0.9rem;
		height: 0.9rem;
		top: 0.5rem;
	}
	.search .pagefind-ui__search-input {
		font-size: 0.9rem;
		padding: 0.35rem 0.5rem 0.35rem 1.8rem;
		background: var(--bg);
		color: var(--fg);
		border: none;
		border-bottom: 1px solid var(--border);
	}
	.search .pagefind-ui__search-input:focus {
		border-bottom-color: var(--fg);
		outline: none;
	}
	.search .pagefind-ui__search-clear {
		background: transparent;
		color: var(--muted);
		font-size: 0.8rem;
	}
	/* Results drop below the input, over the app background */
	.search .pagefind-ui__results-area {
		position: absolute;
		left: 0;
		right: 0;
		background: var(--bg);
		border: 1px solid var(--border);
		padding: 0.5rem 0.75rem;
		max-height: 70vh;
		overflow-y: auto;
		z-index: 20;
	}
	.search .pagefind-ui__result {
		border-top: 1px solid var(--border);
	}
	.search .pagefind-ui__result-title a {
		color: var(--fg);
	}
	.search .pagefind-ui__result-excerpt {
		color: var(--muted);
	}
	.search mark {
		background: transparent;
		color: var(--accent);
		font-weight: 700;
	}
</style>
